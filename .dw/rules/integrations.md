# Integrations - winbox-gui

> Auto-generated by /dw-analyze-project on 2026-05-28

## Project Dependency Graph

```text
src/main.js -> Tauri IPC -> src-tauri/src/lib.rs
src-tauri/src/lib.rs -> src-tauri/src/commands/*
src-tauri/src/commands/* -> src-tauri/src/core/*
src-tauri/src/core/docker.rs -> docker / docker compose
src-tauri/src/core/compose.rs -> profile-local compose.yml
src-tauri/src/core/opener.rs -> default browser -> http://127.0.0.1:<WEB_PORT>
tests/e2e/wdio.conf.mjs -> built Tauri debug binary + tauri-driver
```

## Shared Packages

No monorepo shared packages were detected. The repository is split by language
and runtime boundary rather than publishable packages.

| Package | Path | Used by | Purpose |
|---------|------|---------|---------|
| Frontend helpers | `src/dom-utils.js`, `src/profile-display.js`, `src/i18n.js` | `src/main.js`, unit tests | Escape/render/i18n helpers |
| Rust core modules | `src-tauri/src/core/` | `commands/`, `lib.rs`, `cli.rs` | Docker, Compose, validation, health, paths |

## Service Communication

| From | To | Method | Endpoint/Topic | Description |
|------|----|--------|----------------|-------------|
| Frontend | Rust backend | Tauri IPC | command names in `generate_handler!` | Dashboard/profile operations |
| Rust backend | Frontend | Tauri event | `operation-progress` | Long-running operation updates |
| Rust backend | Docker daemon | CLI process | `docker`, `docker compose` | VM container lifecycle |
| Rust backend | noVNC viewer | Browser launcher | `http://127.0.0.1:<WEB_PORT>` | Opens VM browser viewer |
| Rust backend | Host OS | CLI process | `df`, `du`, `cp`, `xdg-open`, `wsl.exe`, `schtasks.exe`, `pkexec`/`sudo` | Health, snapshots, launch, Windows bootstrap, VFIO |

## Git Submodules

None detected.

## Shared Configuration

| Config | Location | Consumed by |
|--------|----------|-------------|
| npm scripts/dependencies | `package.json` | JS checks/tests, Tauri CLI |
| Rust dependencies/profile | `src-tauri/Cargo.toml` | Rust build/test/bundle |
| Tauri app config | `src-tauri/tauri.conf.json` | Tauri build/runtime |
| Tauri capabilities | `src-tauri/capabilities/default.json` | Tauri permission model |
| CI | `.github/workflows/ci.yml` | GitHub Actions |
| E2E config | `tests/e2e/wdio.conf.mjs` | WebdriverIO smoke tests |

## Build & Deploy Order

1. Install JavaScript dependencies with `npm ci` or `npm install`.
2. Validate frontend syntax with `npm run check:js`.
3. Run frontend unit tests with `npm run test:js`.
4. Validate Rust formatting and lints with Cargo fmt/clippy.
5. Run Rust tests with `cargo test --manifest-path src-tauri/Cargo.toml`.
6. Build/package with `npm run build` / Tauri CLI.

## Integration Contracts to Preserve

- Keep frontend command names aligned with `tauri::generate_handler!`.
- Keep serialized field names stable for frontend DTOs, especially camelCase
  aliases like `imageFamily`, `storagePath`, and `gpuBdf`.
- Keep `operation-progress` payload fields stable: `profile`, `op`, `step`,
  `status`, `message`.
- Keep generated Compose ports bound to `127.0.0.1`.
- Keep profile storage/config path semantics centralized in `core::paths`.
