---
name: dw-plan
description: "Trigger when user has a feature idea and needs spec + architecture + tasks. Runs PRD → TechSpec → Tasks sequentially. Stages: prd / techspec / tasks; --from techspec; --council."
---

Read and follow ALL instructions in `.dw/commands/dw-plan.md`.
